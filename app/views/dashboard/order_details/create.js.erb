$("<%= build_order_detail(@order_detail) %>")
.hide()
.appendTo('#order_details')
.fadeIn(200, function() {
  new App.Utilities.Order();
});

$('#total').text("<%= number_to_currency @order.total, unit: '$' %>")
